
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################


#
# Please note not all available settings / options are set here.
# For a full list, see the wiki (basic and advanced configuring)
#

# autogenerated=1# remove this line to get rid of the warning on top.

monitor=,preferred,auto,1

input {
    kb_layout=be
    kb_variant=
    kb_model=
    kb_options=
    kb_rules=

    follow_mouse=1

    touchpad {
        natural_scroll=yes
    }
}

device:Keychron K8 Pro Keyboard {
    kb_layout=us
}

general {
    sensitivity=1.0 # for mouse cursor
    main_mod=SUPER

    gaps_in=5
    gaps_out=10
    border_size=2
    # col.active_border=0x66ee1111
    col.active_border=0x99f0c6c6
    # col.inactive_border=0x66333333
    col.inactive_border=0x99363a4f

    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)

    damage_tracking=full # leave it on full unless you hate your GPU and want to make it suffer
}

decoration {
    rounding=10
    blur=1
    blur_size=3 # minimum 1
    blur_passes=1 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}

animations {
    enabled=1
    animation=windows,1,7,default
    animation=border,1,10,default
    animation=fade,1,10,default
    animation=workspaces,1,6,default
}

dwindle {
    pseudotile=0 # enable pseudotiling on dwindle
}

gestures {
    workspace_swipe=no
}

# example window rules
# for windows named/classed as abc and xyz
#windowrule=move 69 420,abc
#windowrule=size 420 69,abc
#windowrule=tile,xyz
#windowrule=float,abc
#windowrule=pseudo,abc
#windowrule=monitor 0,xyz
windowrule=float,Rofi
windowrule=float,blueberry.py
windowrule=workspace 1,Alacritty
windowrule=workspace 1,kitty
windowrule=workspace 5,discord
windowrule=workspace 2,firefox
windowrule=workspace 3,thunar
windowrule=workspace 10,Spotify

# example binds
# bind=SUPER,Q,exec,kitty
bind=SUPER,RETURN,exec,kitty
bind=SUPER,W,exec,firefox
bind=SUPER,Q,killactive,
bind=SUPER,O,exit,
bind=SUPER,N,exec,thunar
bind=SUPER,V,togglefloating,
bind=SUPER,P,pseudo,
bind=SUPER,D,exec,rofi -modi drun -show drun
bind=SUPERSHIFT,D,exec,/home/kasper/.local/bin/switcher
bind=SUPERSHIFT,R,exec,rofi -modi run -show run
bind=SUPER,F,fullscreen 
bind=SUPER,M,fullscreen,1
# Lock
bind=SUPERALT,m,exec,swaylock --config ~/.config/swaylock/config
bind=SUPERSHIFT,e,exec,~/.config/scripts/rofi-power

bind=SUPER,left,movefocus,l
bind=SUPER,right,movefocus,r
bind=SUPER,up,movefocus,u
bind=SUPER,down,movefocus,d

bind=SUPER,h,movefocus,l
bind=SUPER,l,movefocus,r
bind=SUPER,k,movefocus,u
bind=SUPER,j,movefocus,d

bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10

bind=SUPER,87,workspace,1
bind=SUPER,88,workspace,2
bind=SUPER,89,workspace,3
bind=SUPER,83,workspace,4
bind=SUPER,84,workspace,5
bind=SUPER,85,workspace,6
bind=SUPER,79,workspace,7
bind=SUPER,80,workspace,8
bind=SUPER,81,workspace,9
bind=SUPER,90,workspace,10

bind=SUPER,ampersand,workspace,1
bind=SUPER,eacute,workspace,2
bind=SUPER,quotedbl,workspace,3
bind=SUPER,apostrophe,workspace,4
bind=SUPER,parenleft,workspace,5
bind=SUPER,section,workspace,6
bind=SUPER,egrave,workspace,7
bind=SUPER,!,workspace,8
bind=SUPER,ccedilla,workspace,9
bind=SUPER,agrave,workspace,10

bind=SUPERSHIFT,10,movetoworkspace,1
bind=SUPERSHIFT,11,movetoworkspace,2
bind=SUPERSHIFT,12,movetoworkspace,3
bind=SUPERSHIFT,13,movetoworkspace,4
bind=SUPERSHIFT,14,movetoworkspace,5
bind=SUPERSHIFT,15,movetoworkspace,6
bind=SUPERSHIFT,16,movetoworkspace,7
bind=SUPERSHIFT,17,movetoworkspace,8
bind=SUPERSHIFT,18,movetoworkspace,9
bind=SUPERSHIFT,19,movetoworkspace,10

bind=SUPERSHIFT,KP_End,movetoworkspace,1
bind=SUPERSHIFT,KP_Down,movetoworkspace,2
bind=SUPERSHIFT,KP_Next,movetoworkspace,3
bind=SUPERSHIFT,KP_Left,movetoworkspace,4
bind=SUPERSHIFT,KP_Begin,movetoworkspace,5
bind=SUPERSHIFT,KP_Right,movetoworkspace,6
bind=SUPERSHIFT,KP_Home,movetoworkspace,7
bind=SUPERSHIFT,KP_Up,movetoworkspace,8
bind=SUPERSHIFT,KP_Prior,movetoworkspace,9
bind=SUPERSHIFT,KP_Insert,movetoworkspace,10

bind=SUPER,mouse_down,workspace,e+1
bind=SUPER,mouse_up,workspace,e-1

# media keys
bind=,XF86AudioPlay,exec,playerctl play-pause
bind=,XF86AudioNext,exec,playerctl next
bind=,XF86AudioPrev,exec,playerctl prev
bind=,XF86AudioLowerVolume,exec,pamixer -d 5
bind=,XF86AudioRaiseVolume,exec,pamixer -i 5
bind=,XF86AudioMute,exec,pamixer -t
bind=,XF86AudioMicMute,exec,amixer set Capture toggle
bind=,XF86MonBrightnessDown,exec,brightnessctl set 5%-
bind=,XF86MonBrightnessUp,exec,brightnessctl set 5%+
bind=,Print,exec,grim -g $(slurp)

# Autostart

exec-once=swaybg -i ~/Images/uchiha\ itachi.png
# exec-once=~/.config/waybar/launch.sh
exec-once=waybar
exec-once=mako
exec-once=nm-applet --indicator
exec-once=discord

# polkit agant

exec=/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# mouse theme
exec-once=hyprctl setcursor Breeze 12

# GTK theme and issues
exec=gsettings set org.gnome.desktop.interface gtk-theme 'Catppuccin-Macchiato'
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=/home/kasper/.scripts/hyprland-gtk.sh
